# Relax push profile.
# Loaded when DAILYANA_PROFILE=relax.
# This currently preserves the existing current style.

content:
  selected_categories:
    - entertainment

notification:
  enabled: true

source_digest:
  enabled: false
  max_items: 30
  dedupe: true
  include_hotlist: true
  include_rss: true

display:
  region_order:
    - new_items
    - hotlist
    - rss
    - standalone
    - ai_analysis
  regions:
    hotlist: true
    new_items: false
    rss: true
    standalone: false
    ai_analysis: true

ai_analysis:
  enabled: true
  include_rss: false
  include_standalone: true
  include_rank_timeline: true
